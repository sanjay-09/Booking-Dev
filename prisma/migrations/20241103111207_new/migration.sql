BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Seat] ADD [version] INT NOT NULL CONSTRAINT [Seat_version_df] DEFAULT 1;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
